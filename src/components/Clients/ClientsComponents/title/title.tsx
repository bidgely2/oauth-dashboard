import styled from "@emotion/styled";
import { Typography } from "@mui/material";

export const Title = styled(Typography)`
    font-family:ibm-plex-sans-medium, 'Arial Narrow', sans-serif;
    font-size: 1.25rem;
    font-weight: 550;
    letter-spacing: 1px;
`;